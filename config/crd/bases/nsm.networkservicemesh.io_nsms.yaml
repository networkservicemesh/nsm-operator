---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.0
  creationTimestamp: null
  name: nsms.nsm.networkservicemesh.io
spec:
  group: nsm.networkservicemesh.io
  names:
    kind: NSM
    listKind: NSMList
    plural: nsms
    singular: nsm
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NSM is the Schema for the nsms API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: NSMSpec defines the desired state of NSM
            properties:
              forwarders:
                items:
                  properties:
                    Image:
                      description: Forwarder image string (must be a complete image
                        path with tag)
                      type: string
                    Name:
                      description: Forwarder descriptive name
                      type: string
                  required:
                  - Image
                  - Name
                  type: object
                type: array
              nsmPullPolicy:
                description: PullPolicy describes a policy for if/when to pull a container
                  image
                type: string
              nsmgrImage:
                type: string
              registry:
                properties:
                  image:
                    description: Registry Image with tag
                    type: string
                  type:
                    description: Registry type
                    enum:
                    - k8s
                    - memory
                    type: string
                required:
                - type
                type: object
              version:
                description: tag represents the desired nsm version
                type: string
            required:
            - forwarders
            - nsmPullPolicy
            - registry
            - version
            type: object
          status:
            description: NSMStatus defines the observed state of NSM
            properties:
              phase:
                description: Operator phases during deployment
                type: string
            required:
            - phase
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
